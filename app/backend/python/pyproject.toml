[project]
name = "server"
version = "0.1.0"
description = "LookBack local vector service"
requires-python = ">=3.14"
dependencies = [
    "pillow",
    "torch",
    "torchvision",
    "transformers",
]
